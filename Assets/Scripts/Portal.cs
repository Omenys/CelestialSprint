using UnityEngine;

public class Portal : MonoBehaviour
{
    public enum Color
    {
        Red = 1,
        Blue = 2,
        Green = 3,
    }

    int portalsEntered = 0;
    int shieldCharges = 0;

    Color color = Color.Red;

    void Spawn()
    {

    }


    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
